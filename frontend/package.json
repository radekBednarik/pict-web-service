{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rimraf ./dist && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
